spring:
  datasource:
    url: jdbc:h2:mem:nutriledger;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
  cache:
    type: none
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
